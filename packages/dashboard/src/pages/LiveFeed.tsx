export function LiveFeed() {
  return (
    <main className="mx-auto max-w-6xl px-4 py-8">
      <h1 className="font-mono text-2xl font-bold">AgentGate</h1>
      <p className="mt-2 text-zinc-400">
        Live feed lands in AG-8 — the money shot (pending approvals + activity feed + stats bar).
      </p>
    </main>
  );
}